# local review/labelling tooling — not part of the pipeline deliverable
label_gt.py
validate_gt.py
review_frames.py
diagnose_baseline.py
diagnose_coverage.py
tune_stitch.py
classify_tracks.py
define_roi.py
eval/label/no_overlap_review.yaml

# large binaries and derived artifacts — not committed
digital_kiosk.mp4
yolov8n.pt
yolov8s.pt
yolov8m.pt
rtdetr-l.pt
FastSAM-s.pt
# overlay render outputs (large video / per-frame artifact, regenerated on demand)
*.mp4
outputs/**/render_frames.yaml
outputs/render_frames.yaml
outputs/**/appearances.npz
outputs/**/stitch_state.pkl
# boxmot ReID backbone weights (auto-downloaded on first run, like yolov8n.pt)
weights/

# local boxmot sanity-check virtualenv
.venv-boxmot/
# RF-DETR virtualenv: rfdetr needs transformers>=5.1, which conflicts with the
# transformers==4.45.2 the RT-DETR wrapper pins, so it is isolated (see decisions.md)
.venv-rfdetr/

# cached detector output (dump_detections.py) — large binary, regenerated on demand
outputs/det_cache/
# per-point sweep run dirs — the sweep_rows.json summary is the artifact worth keeping
outputs/sweep_*/*/
outputs/bakeoff_*/*/
outputs/baseline_*/*/
outputs/v2_final/*/
.DS_Store


# python caches
__pycache__/
.pytest_cache/
